---
name: slgs-permissions
description: SLGS project-specific engineering skill.
---

# slgs-permissions

Use explicit domain:action[:scope] permissions. Deny by default, enforce server-side, apply least privilege, scope records where necessary, and audit sensitive mutations.
