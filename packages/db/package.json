{
  "name": "@slgs/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:verify:phase1a": "psql --set=ON_ERROR_STOP=1 \"$DATABASE_MIGRATION_URL\" --file verification/phase-1a.sql",
    "db:verify:phase1c": "psql --set=ON_ERROR_STOP=1 \"$DATABASE_MIGRATION_URL\" --file verification/phase-1c.sql",
    "lint": "eslint src drizzle.config.ts",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@slgs/validation": "workspace:*",
    "drizzle-orm": "catalog:",
    "postgres": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "vitest": "catalog:"
  }
}
